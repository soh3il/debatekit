/**
 * @debatekit/eslint-config
 * Shared ESLint configuration for the monorepo
 */
export { createApiConfig } from './api';
export { createConfig } from './base';
export { createSharedConfig } from './shared';
export { createWebConfig } from './web';
