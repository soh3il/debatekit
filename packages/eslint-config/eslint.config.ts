/**
 * ESLint configuration for the eslint-config package itself
 * Uses createConfig to stay consistent with the root config
 */
import { createConfig } from './src/base';

export default createConfig();
