/**
 * Application Version
 *
 * This is a fallback. Apps should use their own version.ts which gets
 * the version injected at build time from root package.json.
 */

export const APP_VERSION = '0.0.0-shared';
