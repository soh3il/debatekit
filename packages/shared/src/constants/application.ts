/**
 * Pagination Configuration
 *
 * SINGLE SOURCE OF TRUTH: Pagination limits used in API schemas and pagination utilities
 */

export const API = {
  DEFAULT_PAGE_SIZE: 20,
  MAX_PAGE_SIZE: 100,
} as const;
